# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DocuSignChallenge' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

    pod 'Alamofire'
    pod 'SwiftyJSON'
    pod 'ObjectMapper'
    pod 'SDWebImage', '~> 4.0'

  # Pods for DocuSignChallenge

  target 'DocuSignChallengeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DocuSignChallengeUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
